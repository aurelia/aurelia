---
"@aurelia/ui-virtualization": minor
---

Add a `gap` option to `virtual-repeat` for layouts with fixed spacing between items. Configure the gap separately from `item-height` or `item-width`; vertical and horizontal spacer and scroll-position calculations now account for that spacing.

pr: #2409
