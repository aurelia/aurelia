---
"@aurelia/runtime": patch
---

Computed getter callback dependencies now behave consistently in shorthand and options forms.

Bare `@computed()` getter declarations now retain automatic dependency tracking.
