{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["**/*.ts", "**/*.tsx", "esbuild.dev.cjs", "!z-scripts", "tsconfig.json"],
      "outputs": ["dist/**/*"]
    }
  }
}
