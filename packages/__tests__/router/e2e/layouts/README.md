# jit-browserify-ts

## Getting started

To install and run the app (in watch mode):

```bash
npm install
npm run watch
```

To install and run the app (in "prod" mode):

```bash
npm install
npm run build
npm run serve
```
