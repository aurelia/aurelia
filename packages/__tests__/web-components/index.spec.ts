import { placeholder } from '@aurelia/web-components';
import { assert } from '@aurelia/testing';

describe('placeholder', function () {
  it('is null', function () {
    assert.strictEqual(placeholder, null);
  });
});
