import { expect } from "chai";
import { DI } from "../../../kernel/src/index";
import { CustomElementResource, DOM, Aurelia } from "../../../runtime/src/index";
import { BasicConfiguration } from "../../src/index";

describe("generated.template-compiler.static.if-else.repeat.double", function generated_template_compiler_static_if_else_repeat_double() {
    function setup() {
        const container = DI.createContainer();
        container.register(BasicConfiguration);
        const au = new Aurelia(container);
        const host = DOM.createElement("div");
        return { au, host };
    }
    it("tag$01 text$01 if$01 repeat$11 variant$01$double _", function tag$01_text$01_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else if.bind=\"true\">b</div><div if.bind=\"true\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$02$double _", function tag$01_text$01_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 1\">b</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$03$double$01 _", function tag$01_text$01_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else if.bind=\"true\" repeat.for=\"i of 1\">b</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$03$double$02 _", function tag$01_text$01_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\" if.bind=\"true\">b</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$09$double _", function tag$01_text$01_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$01$double _", function tag$01_text$01_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else if.bind=\"true\">b</div><div if.bind=\"true\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$02$double _", function tag$01_text$01_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 3\">b</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$03$double$01 _", function tag$01_text$01_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else if.bind=\"true\" repeat.for=\"i of 3\">b</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$03$double$02 _", function tag$01_text$01_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\" if.bind=\"true\">b</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$09$double _", function tag$01_text$01_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$01$double _", function tag$01_text$01_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else if.bind=\"false\">b</div><div if.bind=\"false\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$02$double _", function tag$01_text$01_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 1\">b</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$03$double$01 _", function tag$01_text$01_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else if.bind=\"false\" repeat.for=\"i of 1\">b</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$03$double$02 _", function tag$01_text$01_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\" if.bind=\"false\">b</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$11$double _", function tag$01_text$01_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">b</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$12$double$01 _", function tag$01_text$01_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\" if.bind=\"false\">b</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$12$double$02 _", function tag$01_text$01_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 1\">b</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$01$double _", function tag$01_text$01_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else if.bind=\"false\">b</div><div if.bind=\"false\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$02$double _", function tag$01_text$01_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 3\">b</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$03$double$01 _", function tag$01_text$01_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else if.bind=\"false\" repeat.for=\"i of 3\">b</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$03$double$02 _", function tag$01_text$01_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\" if.bind=\"false\">b</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$11$double _", function tag$01_text$01_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">b</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$12$double$01 _", function tag$01_text$01_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\" if.bind=\"false\">b</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$12$double$02 _", function tag$01_text$01_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 3\">b</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$01$double _", function tag$01_text$02_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$02$double _", function tag$01_text$02_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$03$double$01 _", function tag$01_text$02_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$03$double$02 _", function tag$01_text$02_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$09$double _", function tag$01_text$02_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$01$double _", function tag$01_text$02_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$02$double _", function tag$01_text$02_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$03$double$01 _", function tag$01_text$02_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$03$double$02 _", function tag$01_text$02_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$09$double _", function tag$01_text$02_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$01$double _", function tag$01_text$02_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$02$double _", function tag$01_text$02_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">a</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$03$double$01 _", function tag$01_text$02_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$03$double$02 _", function tag$01_text$02_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$11$double _", function tag$01_text$02_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$12$double$01 _", function tag$01_text$02_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$12$double$02 _", function tag$01_text$02_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$01$double _", function tag$01_text$02_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$02$double _", function tag$01_text$02_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">a</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$03$double$01 _", function tag$01_text$02_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$03$double$02 _", function tag$01_text$02_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$11$double _", function tag$01_text$02_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$12$double$01 _", function tag$01_text$02_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$12$double$02 _", function tag$01_text$02_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$01$double _", function tag$01_text$03_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$02$double _", function tag$01_text$03_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$03$double$01 _", function tag$01_text$03_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$03$double$02 _", function tag$01_text$03_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$09$double _", function tag$01_text$03_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$01$double _", function tag$01_text$03_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$02$double _", function tag$01_text$03_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$03$double$01 _", function tag$01_text$03_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$03$double$02 _", function tag$01_text$03_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$09$double _", function tag$01_text$03_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$01$double _", function tag$01_text$03_if$01_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else if.bind=\"true\">${item}</div><div if.bind=\"true\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$02$double _", function tag$01_text$03_if$01_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$03$double$01 _", function tag$01_text$03_if$01_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$03$double$02 _", function tag$01_text$03_if$01_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</div><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$09$double _", function tag$01_text$03_if$01_repeat$13_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$01$double _", function tag$01_text$03_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$02$double _", function tag$01_text$03_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$03$double$01 _", function tag$01_text$03_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$03$double$02 _", function tag$01_text$03_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$11$double _", function tag$01_text$03_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$12$double$01 _", function tag$01_text$03_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$12$double$02 _", function tag$01_text$03_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$01$double _", function tag$01_text$03_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$02$double _", function tag$01_text$03_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$03$double$01 _", function tag$01_text$03_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$03$double$02 _", function tag$01_text$03_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$11$double _", function tag$01_text$03_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$12$double$01 _", function tag$01_text$03_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$12$double$02 _", function tag$01_text$03_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$01$double _", function tag$01_text$03_if$02_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else if.bind=\"false\">${item}</div><div if.bind=\"false\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$02$double _", function tag$01_text$03_if$02_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$03$double$01 _", function tag$01_text$03_if$02_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$03$double$02 _", function tag$01_text$03_if$02_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</div><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$11$double _", function tag$01_text$03_if$02_repeat$13_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$12$double$01 _", function tag$01_text$03_if$02_repeat$13_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</div><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$12$double$02 _", function tag$01_text$03_if$02_repeat$13_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$01$double _", function tag$01_text$04_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$02$double _", function tag$01_text$04_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$03$double$01 _", function tag$01_text$04_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$03$double$02 _", function tag$01_text$04_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$09$double _", function tag$01_text$04_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$01$double _", function tag$01_text$04_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else if.bind=\"true\">${notMsg}</div><div if.bind=\"true\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$02$double _", function tag$01_text$04_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"true\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$03$double$01 _", function tag$01_text$04_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$03$double$02 _", function tag$01_text$04_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</div><div if.bind=\"true\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$09$double _", function tag$01_text$04_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$01$double _", function tag$01_text$04_if$01_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else if.bind=\"true\">${item}</div><div if.bind=\"true\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$02$double _", function tag$01_text$04_if$01_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"true\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$03$double$01 _", function tag$01_text$04_if$01_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$03$double$02 _", function tag$01_text$04_if$01_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</div><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$09$double _", function tag$01_text$04_if$01_repeat$13_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"true\"></div><div if.bind=\"true\"></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$01$double _", function tag$01_text$04_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$02$double _", function tag$01_text$04_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">${msg}</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$03$double$01 _", function tag$01_text$04_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$03$double$02 _", function tag$01_text$04_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 1\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$11$double _", function tag$01_text$04_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$12$double$01 _", function tag$01_text$04_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$12$double$02 _", function tag$01_text$04_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 1\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$01$double _", function tag$01_text$04_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else>${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$02$double _", function tag$01_text$04_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">${msg}</div></div><div else if.bind=\"false\"><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$03$double$01 _", function tag$01_text$04_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$03$double$02 _", function tag$01_text$04_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\" repeat.for=\"i of 3\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$11$double _", function tag$01_text$04_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${notMsg}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$12$double$01 _", function tag$01_text$04_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$12$double$02 _", function tag$01_text$04_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</div><div if.bind=\"false\"></div><div else repeat.for=\"i of 3\">${notMsg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$01$double _", function tag$01_text$04_if$02_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else if.bind=\"false\">${item}</div><div if.bind=\"false\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$02$double _", function tag$01_text$04_if$02_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$03$double$01 _", function tag$01_text$04_if$02_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$03$double$02 _", function tag$01_text$04_if$02_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</div><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$11$double _", function tag$01_text$04_if$02_repeat$13_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div if.bind=\"false\" else></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$12$double$01 _", function tag$01_text$04_if$02_repeat$13_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</div><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$12$double$02 _", function tag$01_text$04_if$02_repeat$13_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div if.bind=\"false\"></div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$01$double _", function tag$02_text$01_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else if.bind=\"true\">b</template><template if.bind=\"true\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$02$double _", function tag$02_text$01_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 1\">b</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$03$double$01 _", function tag$02_text$01_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else if.bind=\"true\" repeat.for=\"i of 1\">b</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$03$double$02 _", function tag$02_text$01_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\" if.bind=\"true\">b</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$09$double _", function tag$02_text$01_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$01$double _", function tag$02_text$01_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else if.bind=\"true\">b</template><template if.bind=\"true\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$02$double _", function tag$02_text$01_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 3\">b</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$03$double$01 _", function tag$02_text$01_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else if.bind=\"true\" repeat.for=\"i of 3\">b</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$03$double$02 _", function tag$02_text$01_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\" if.bind=\"true\">b</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$09$double _", function tag$02_text$01_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$01$double _", function tag$02_text$01_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else if.bind=\"false\">b</template><template if.bind=\"false\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$02$double _", function tag$02_text$01_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 1\">b</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$03$double$01 _", function tag$02_text$01_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else if.bind=\"false\" repeat.for=\"i of 1\">b</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$03$double$02 _", function tag$02_text$01_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\" if.bind=\"false\">b</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$11$double _", function tag$02_text$01_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">b</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$12$double$01 _", function tag$02_text$01_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\" if.bind=\"false\">b</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$12$double$02 _", function tag$02_text$01_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 1\">b</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$01$double _", function tag$02_text$01_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else if.bind=\"false\">b</template><template if.bind=\"false\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$02$double _", function tag$02_text$01_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 3\">b</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$03$double$01 _", function tag$02_text$01_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else if.bind=\"false\" repeat.for=\"i of 3\">b</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$03$double$02 _", function tag$02_text$01_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\" if.bind=\"false\">b</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$11$double _", function tag$02_text$01_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">b</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$12$double$01 _", function tag$02_text$01_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\" if.bind=\"false\">b</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$12$double$02 _", function tag$02_text$01_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 3\">b</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$01$double _", function tag$02_text$02_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$02$double _", function tag$02_text$02_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$03$double$01 _", function tag$02_text$02_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$03$double$02 _", function tag$02_text$02_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$09$double _", function tag$02_text$02_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$01$double _", function tag$02_text$02_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$02$double _", function tag$02_text$02_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$03$double$01 _", function tag$02_text$02_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$03$double$02 _", function tag$02_text$02_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$09$double _", function tag$02_text$02_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$01$double _", function tag$02_text$02_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$02$double _", function tag$02_text$02_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">a</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$03$double$01 _", function tag$02_text$02_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$03$double$02 _", function tag$02_text$02_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$11$double _", function tag$02_text$02_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$12$double$01 _", function tag$02_text$02_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$12$double$02 _", function tag$02_text$02_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$01$double _", function tag$02_text$02_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$02$double _", function tag$02_text$02_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">a</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$03$double$01 _", function tag$02_text$02_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$03$double$02 _", function tag$02_text$02_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$11$double _", function tag$02_text$02_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$12$double$01 _", function tag$02_text$02_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$12$double$02 _", function tag$02_text$02_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$01$double _", function tag$02_text$03_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$02$double _", function tag$02_text$03_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$03$double$01 _", function tag$02_text$03_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$03$double$02 _", function tag$02_text$03_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$09$double _", function tag$02_text$03_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$01$double _", function tag$02_text$03_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$02$double _", function tag$02_text$03_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$03$double$01 _", function tag$02_text$03_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$03$double$02 _", function tag$02_text$03_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$09$double _", function tag$02_text$03_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$01$double _", function tag$02_text$03_if$01_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else if.bind=\"true\">${item}</template><template if.bind=\"true\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$02$double _", function tag$02_text$03_if$01_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$03$double$01 _", function tag$02_text$03_if$01_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$03$double$02 _", function tag$02_text$03_if$01_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$09$double _", function tag$02_text$03_if$01_repeat$13_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$01$double _", function tag$02_text$03_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$02$double _", function tag$02_text$03_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$03$double$01 _", function tag$02_text$03_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$03$double$02 _", function tag$02_text$03_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$11$double _", function tag$02_text$03_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$12$double$01 _", function tag$02_text$03_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$12$double$02 _", function tag$02_text$03_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$01$double _", function tag$02_text$03_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$02$double _", function tag$02_text$03_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$03$double$01 _", function tag$02_text$03_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$03$double$02 _", function tag$02_text$03_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$11$double _", function tag$02_text$03_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$12$double$01 _", function tag$02_text$03_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$12$double$02 _", function tag$02_text$03_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$01$double _", function tag$02_text$03_if$02_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else if.bind=\"false\">${item}</template><template if.bind=\"false\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$02$double _", function tag$02_text$03_if$02_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$03$double$01 _", function tag$02_text$03_if$02_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$03$double$02 _", function tag$02_text$03_if$02_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$11$double _", function tag$02_text$03_if$02_repeat$13_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$12$double$01 _", function tag$02_text$03_if$02_repeat$13_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$12$double$02 _", function tag$02_text$03_if$02_repeat$13_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$01$double _", function tag$02_text$04_if$01_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$02$double _", function tag$02_text$04_if$01_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$03$double$01 _", function tag$02_text$04_if$01_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else if.bind=\"true\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$03$double$02 _", function tag$02_text$04_if$01_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$09$double _", function tag$02_text$04_if$01_repeat$11_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$01$double _", function tag$02_text$04_if$01_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else if.bind=\"true\">${notMsg}</template><template if.bind=\"true\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$02$double _", function tag$02_text$04_if$01_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"true\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$03$double$01 _", function tag$02_text$04_if$01_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else if.bind=\"true\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$03$double$02 _", function tag$02_text$04_if$01_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\" if.bind=\"true\">${notMsg}</template><template if.bind=\"true\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$09$double _", function tag$02_text$04_if$01_repeat$12_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$01$double _", function tag$02_text$04_if$01_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else if.bind=\"true\">${item}</template><template if.bind=\"true\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$02$double _", function tag$02_text$04_if$01_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"true\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$03$double$01 _", function tag$02_text$04_if$01_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$03$double$02 _", function tag$02_text$04_if$01_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$09$double _", function tag$02_text$04_if$01_repeat$13_variant$09$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"true\"></template><template if.bind=\"true\"></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$01$double _", function tag$02_text$04_if$02_repeat$11_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$02$double _", function tag$02_text$04_if$02_repeat$11_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">${msg}</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$03$double$01 _", function tag$02_text$04_if$02_repeat$11_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$03$double$02 _", function tag$02_text$04_if$02_repeat$11_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 1\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$11$double _", function tag$02_text$04_if$02_repeat$11_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$12$double$01 _", function tag$02_text$04_if$02_repeat$11_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$12$double$02 _", function tag$02_text$04_if$02_repeat$11_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 1\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 1\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$01$double _", function tag$02_text$04_if$02_repeat$12_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else>${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$02$double _", function tag$02_text$04_if$02_repeat$12_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">${msg}</template></template><template else if.bind=\"false\"><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$03$double$01 _", function tag$02_text$04_if$02_repeat$12_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$03$double$02 _", function tag$02_text$04_if$02_repeat$12_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\" repeat.for=\"i of 3\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$11$double _", function tag$02_text$04_if$02_repeat$12_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${notMsg}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$12$double$01 _", function tag$02_text$04_if$02_repeat$12_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\" if.bind=\"false\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$12$double$02 _", function tag$02_text$04_if$02_repeat$12_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"i of 3\">${notMsg}</template><template if.bind=\"false\"></template><template else repeat.for=\"i of 3\">${notMsg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$01$double _", function tag$02_text$04_if$02_repeat$13_variant$01$double__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else if.bind=\"false\">${item}</template><template if.bind=\"false\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$02$double _", function tag$02_text$04_if$02_repeat$13_variant$02$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$03$double$01 _", function tag$02_text$04_if$02_repeat$13_variant$03$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$03$double$02 _", function tag$02_text$04_if$02_repeat$13_variant$03$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$11$double _", function tag$02_text$04_if$02_repeat$13_variant$11$double__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template if.bind=\"false\" else></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$12$double$01 _", function tag$02_text$04_if$02_repeat$13_variant$12$double$01__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"false\">${item}</template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$12$double$02 _", function tag$02_text$04_if$02_repeat$13_variant$12$double$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template if.bind=\"false\"></template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            notMsg = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
});