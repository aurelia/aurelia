import { expect } from "chai";
import { DI } from "../../../kernel/src/index";
import { CustomElementResource, DOM, Aurelia, BindingMode } from "../../../runtime/src/index";
import { BasicConfiguration } from "../../src/index";

describe("generated.template-compiler.static.if-else.repeat", function generated_template_compiler_static_if_else_repeat() {
    function setup() {
        const container = DI.createContainer();
        container.register(BasicConfiguration);
        const au = new Aurelia(container);
        const host = DOM.createElement("div");
        return { au, host };
    }
    it("tag$01 text$01 if$01 repeat$11 variant$01 _", function tag$01_text$01_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$02 _", function tag$01_text$01_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$03 _", function tag$01_text$01_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$04 _", function tag$01_text$01_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$05 _", function tag$01_text$01_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\" if.bind=\"true\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$06 _", function tag$01_text$01_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$07 _", function tag$01_text$01_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$08 _", function tag$01_text$01_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$08$empty _", function tag$01_text$01_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$09 _", function tag$01_text$01_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$11 variant$09$empty _", function tag$01_text$01_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$01 _", function tag$01_text$01_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$02 _", function tag$01_text$01_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$03 _", function tag$01_text$01_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$04 _", function tag$01_text$01_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$05 _", function tag$01_text$01_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\" if.bind=\"true\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$06 _", function tag$01_text$01_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$07 _", function tag$01_text$01_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$08 _", function tag$01_text$01_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$08$empty _", function tag$01_text$01_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$09 _", function tag$01_text$01_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$01 repeat$12 variant$09$empty _", function tag$01_text$01_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$01 _", function tag$01_text$01_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$02 _", function tag$01_text$01_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$03 _", function tag$01_text$01_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$10 _", function tag$01_text$01_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$10$empty _", function tag$01_text$01_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$11 _", function tag$01_text$01_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$11$empty _", function tag$01_text$01_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$11 variant$12 _", function tag$01_text$01_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else repeat.for=\"i of 1\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$01 _", function tag$01_text$01_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$02 _", function tag$01_text$01_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$03 _", function tag$01_text$01_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$10 _", function tag$01_text$01_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$10$empty _", function tag$01_text$01_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\"></div><div else>b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$11 _", function tag$01_text$01_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$11$empty _", function tag$01_text$01_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">b</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$01 if$02 repeat$12 variant$12 _", function tag$01_text$01_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else repeat.for=\"i of 3\">b</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$01 _", function tag$01_text$02_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$02 _", function tag$01_text$02_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$03 _", function tag$01_text$02_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$04 _", function tag$01_text$02_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$05 _", function tag$01_text$02_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\" if.bind=\"true\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$06 _", function tag$01_text$02_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$07 _", function tag$01_text$02_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$08 _", function tag$01_text$02_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$08$empty _", function tag$01_text$02_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">a</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$09 _", function tag$01_text$02_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$11 variant$09$empty _", function tag$01_text$02_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">a</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$01 _", function tag$01_text$02_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$02 _", function tag$01_text$02_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$03 _", function tag$01_text$02_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$04 _", function tag$01_text$02_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$05 _", function tag$01_text$02_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\" if.bind=\"true\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$06 _", function tag$01_text$02_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$07 _", function tag$01_text$02_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">a</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$08 _", function tag$01_text$02_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$08$empty _", function tag$01_text$02_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">a</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$09 _", function tag$01_text$02_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">a</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$01 repeat$12 variant$09$empty _", function tag$01_text$02_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">a</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$01 _", function tag$01_text$02_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$02 _", function tag$01_text$02_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">a</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$03 _", function tag$01_text$02_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">a</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$10 _", function tag$01_text$02_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$10$empty _", function tag$01_text$02_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$11 _", function tag$01_text$02_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$11$empty _", function tag$01_text$02_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$11 variant$12 _", function tag$01_text$02_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$01 _", function tag$01_text$02_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$02 _", function tag$01_text$02_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">a</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$03 _", function tag$01_text$02_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">a</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$10 _", function tag$01_text$02_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">a</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$10$empty _", function tag$01_text$02_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$11 _", function tag$01_text$02_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$11$empty _", function tag$01_text$02_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$02 if$02 repeat$12 variant$12 _", function tag$01_text$02_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">a</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$01 _", function tag$01_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$02 _", function tag$01_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$03 _", function tag$01_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$04 _", function tag$01_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$05 _", function tag$01_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\" if.bind=\"true\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$06 _", function tag$01_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$07 _", function tag$01_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$08 _", function tag$01_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$08$empty _", function tag$01_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$09 _", function tag$01_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$11 variant$09$empty _", function tag$01_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$01 _", function tag$01_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$02 _", function tag$01_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$03 _", function tag$01_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$04 _", function tag$01_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$05 _", function tag$01_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\" if.bind=\"true\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$06 _", function tag$01_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$07 _", function tag$01_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$08 _", function tag$01_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$08$empty _", function tag$01_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$09 _", function tag$01_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$12 variant$09$empty _", function tag$01_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$01 _", function tag$01_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$02 _", function tag$01_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$03 _", function tag$01_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$04 _", function tag$01_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$05 _", function tag$01_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$06 _", function tag$01_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$07 _", function tag$01_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$08 _", function tag$01_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$08$empty _", function tag$01_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$09 _", function tag$01_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$01 repeat$13 variant$09$empty _", function tag$01_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$01 _", function tag$01_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$02 _", function tag$01_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$03 _", function tag$01_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$10 _", function tag$01_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$10$empty _", function tag$01_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$11 _", function tag$01_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$11$empty _", function tag$01_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$11 variant$12 _", function tag$01_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$01 _", function tag$01_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$02 _", function tag$01_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$03 _", function tag$01_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$10 _", function tag$01_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$10$empty _", function tag$01_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$11 _", function tag$01_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$11$empty _", function tag$01_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$12 variant$12 _", function tag$01_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$01 _", function tag$01_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$02 _", function tag$01_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$03 _", function tag$01_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$10 _", function tag$01_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$10$empty _", function tag$01_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$11 _", function tag$01_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${item}</div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$11$empty _", function tag$01_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$03 if$02 repeat$13 variant$12 _", function tag$01_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$01 _", function tag$01_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$02 _", function tag$01_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$03 _", function tag$01_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$04 _", function tag$01_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$05 _", function tag$01_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\" if.bind=\"true\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$06 _", function tag$01_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$07 _", function tag$01_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 1\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$08 _", function tag$01_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$08$empty _", function tag$01_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"true\">${msg}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$09 _", function tag$01_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$11 variant$09$empty _", function tag$01_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 1\">${msg}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$01 _", function tag$01_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$02 _", function tag$01_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$03 _", function tag$01_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$04 _", function tag$01_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$05 _", function tag$01_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\" if.bind=\"true\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$06 _", function tag$01_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$07 _", function tag$01_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"i of 3\">${msg}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$08 _", function tag$01_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$08$empty _", function tag$01_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"true\">${msg}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$09 _", function tag$01_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${msg}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$12 variant$09$empty _", function tag$01_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"i of 3\">${msg}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$01 _", function tag$01_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$02 _", function tag$01_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$03 _", function tag$01_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$04 _", function tag$01_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$05 _", function tag$01_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$06 _", function tag$01_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$07 _", function tag$01_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$08 _", function tag$01_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$08$empty _", function tag$01_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"true\">${item}</div><div else></div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$09 _", function tag$01_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$01 repeat$13 variant$09$empty _", function tag$01_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"true\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$01 _", function tag$01_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$02 _", function tag$01_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 1\">${msg}</div></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$03 _", function tag$01_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 1\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$10 _", function tag$01_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$10$empty _", function tag$01_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 1\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$11 _", function tag$01_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$11$empty _", function tag$01_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 1\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$11 variant$12 _", function tag$01_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else repeat.for=\"i of 1\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$01 _", function tag$01_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$02 _", function tag$01_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"i of 3\">${msg}</div></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$03 _", function tag$01_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"i of 3\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$10 _", function tag$01_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\">${msg}</div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$10$empty _", function tag$01_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"i of 3\"><div if.bind=\"false\"></div><div else>${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$11 _", function tag$01_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$11$empty _", function tag$01_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"i of 3\">${not}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$12 variant$12 _", function tag$01_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${msg}</div><div else repeat.for=\"i of 3\">${not}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$01 _", function tag$01_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$02 _", function tag$01_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$03 _", function tag$01_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$10 _", function tag$01_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\">${item}</div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$10$empty _", function tag$01_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><div repeat.for=\"item of ['a', 'b', 'c']\"><div if.bind=\"false\"></div><div else>${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$11 _", function tag$01_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${item}</div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$11$empty _", function tag$01_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\"></div><div else><div repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$01 text$04 if$02 repeat$13 variant$12 _", function tag$01_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><div if.bind=\"false\">${item}</div><div else repeat.for=\"item of ['a', 'b', 'c']\">${item}</div></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$01 _", function tag$02_text$01_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$02 _", function tag$02_text$01_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$03 _", function tag$02_text$01_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$04 _", function tag$02_text$01_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$05 _", function tag$02_text$01_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$06 _", function tag$02_text$01_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$07 _", function tag$02_text$01_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$08 _", function tag$02_text$01_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$08$empty _", function tag$02_text$01_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$09 _", function tag$02_text$01_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$11 variant$09$empty _", function tag$02_text$01_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$01 _", function tag$02_text$01_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$02 _", function tag$02_text$01_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$03 _", function tag$02_text$01_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$04 _", function tag$02_text$01_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$05 _", function tag$02_text$01_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$06 _", function tag$02_text$01_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$07 _", function tag$02_text$01_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$08 _", function tag$02_text$01_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$08$empty _", function tag$02_text$01_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$09 _", function tag$02_text$01_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$01 repeat$12 variant$09$empty _", function tag$02_text$01_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$01 _", function tag$02_text$01_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$02 _", function tag$02_text$01_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$03 _", function tag$02_text$01_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$10 _", function tag$02_text$01_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$10$empty _", function tag$02_text$01_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$11 _", function tag$02_text$01_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$11$empty _", function tag$02_text$01_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$11 variant$12 _", function tag$02_text$01_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else repeat.for=\"i of 1\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$01 _", function tag$02_text$01_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$02 _", function tag$02_text$01_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$03 _", function tag$02_text$01_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$10 _", function tag$02_text$01_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$10$empty _", function tag$02_text$01_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else>b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$11 _", function tag$02_text$01_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$11$empty _", function tag$02_text$01_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">b</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$01 if$02 repeat$12 variant$12 _", function tag$02_text$01_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else repeat.for=\"i of 3\">b</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$01 _", function tag$02_text$02_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$02 _", function tag$02_text$02_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$03 _", function tag$02_text$02_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$04 _", function tag$02_text$02_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$05 _", function tag$02_text$02_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$06 _", function tag$02_text$02_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$07 _", function tag$02_text$02_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$08 _", function tag$02_text$02_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$08$empty _", function tag$02_text$02_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">a</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$09 _", function tag$02_text$02_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$11 variant$09$empty _", function tag$02_text$02_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">a</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$01 _", function tag$02_text$02_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$02 _", function tag$02_text$02_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$03 _", function tag$02_text$02_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$04 _", function tag$02_text$02_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$05 _", function tag$02_text$02_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$06 _", function tag$02_text$02_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$07 _", function tag$02_text$02_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">a</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$08 _", function tag$02_text$02_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$08$empty _", function tag$02_text$02_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">a</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$09 _", function tag$02_text$02_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">a</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$01 repeat$12 variant$09$empty _", function tag$02_text$02_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">a</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$01 _", function tag$02_text$02_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$02 _", function tag$02_text$02_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">a</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$03 _", function tag$02_text$02_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">a</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$10 _", function tag$02_text$02_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$10$empty _", function tag$02_text$02_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$11 _", function tag$02_text$02_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$11$empty _", function tag$02_text$02_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$11 variant$12 _", function tag$02_text$02_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$01 _", function tag$02_text$02_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$02 _", function tag$02_text$02_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">a</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$03 _", function tag$02_text$02_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">a</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$10 _", function tag$02_text$02_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">a</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$10$empty _", function tag$02_text$02_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$11 _", function tag$02_text$02_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$11$empty _", function tag$02_text$02_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$02 if$02 repeat$12 variant$12 _", function tag$02_text$02_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">a</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$01 _", function tag$02_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$02 _", function tag$02_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$03 _", function tag$02_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$04 _", function tag$02_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$05 _", function tag$02_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$06 _", function tag$02_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$07 _", function tag$02_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$08 _", function tag$02_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$08$empty _", function tag$02_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$09 _", function tag$02_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$11 variant$09$empty _", function tag$02_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$01 _", function tag$02_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$02 _", function tag$02_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$03 _", function tag$02_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$04 _", function tag$02_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$05 _", function tag$02_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$06 _", function tag$02_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$07 _", function tag$02_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$08 _", function tag$02_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$08$empty _", function tag$02_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$09 _", function tag$02_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$12 variant$09$empty _", function tag$02_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$01 _", function tag$02_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$02 _", function tag$02_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$03 _", function tag$02_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$04 _", function tag$02_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$05 _", function tag$02_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$06 _", function tag$02_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$07 _", function tag$02_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$08 _", function tag$02_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$08$empty _", function tag$02_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$09 _", function tag$02_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$01 repeat$13 variant$09$empty _", function tag$02_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$01 _", function tag$02_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$02 _", function tag$02_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$03 _", function tag$02_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$10 _", function tag$02_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$10$empty _", function tag$02_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$11 _", function tag$02_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$11$empty _", function tag$02_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$11 variant$12 _", function tag$02_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$01 _", function tag$02_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$02 _", function tag$02_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$03 _", function tag$02_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$10 _", function tag$02_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$10$empty _", function tag$02_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$11 _", function tag$02_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$11$empty _", function tag$02_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$12 variant$12 _", function tag$02_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$01 _", function tag$02_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$02 _", function tag$02_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$03 _", function tag$02_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$10 _", function tag$02_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$10$empty _", function tag$02_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$11 _", function tag$02_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${item}</template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$11$empty _", function tag$02_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$03 if$02 repeat$13 variant$12 _", function tag$02_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$01 _", function tag$02_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$02 _", function tag$02_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$03 _", function tag$02_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$04 _", function tag$02_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$05 _", function tag$02_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$06 _", function tag$02_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$07 _", function tag$02_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$08 _", function tag$02_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$08$empty _", function tag$02_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\">${msg}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$09 _", function tag$02_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$11 variant$09$empty _", function tag$02_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\">${msg}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$01 _", function tag$02_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$02 _", function tag$02_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$03 _", function tag$02_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$04 _", function tag$02_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$05 _", function tag$02_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$06 _", function tag$02_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$07 _", function tag$02_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\">${msg}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$08 _", function tag$02_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$08$empty _", function tag$02_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\">${msg}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$09 _", function tag$02_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${msg}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$12 variant$09$empty _", function tag$02_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\">${msg}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$01 _", function tag$02_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$02 _", function tag$02_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$03 _", function tag$02_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$04 _", function tag$02_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$05 _", function tag$02_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$06 _", function tag$02_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$07 _", function tag$02_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$08 _", function tag$02_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$08$empty _", function tag$02_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\">${item}</template><template else></template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$09 _", function tag$02_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$01 repeat$13 variant$09$empty _", function tag$02_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$01 _", function tag$02_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$02 _", function tag$02_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\">${msg}</template></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$03 _", function tag$02_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$10 _", function tag$02_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$10$empty _", function tag$02_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$11 _", function tag$02_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$11$empty _", function tag$02_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$11 variant$12 _", function tag$02_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else repeat.for=\"i of 1\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$01 _", function tag$02_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$02 _", function tag$02_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\">${msg}</template></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$03 _", function tag$02_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$10 _", function tag$02_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\">${msg}</template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$10$empty _", function tag$02_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else>${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$11 _", function tag$02_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$11$empty _", function tag$02_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\">${not}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$12 variant$12 _", function tag$02_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${msg}</template><template else repeat.for=\"i of 3\">${not}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$01 _", function tag$02_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$02 _", function tag$02_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$03 _", function tag$02_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$10 _", function tag$02_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\">${item}</template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$10$empty _", function tag$02_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else>${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$11 _", function tag$02_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${item}</template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$11$empty _", function tag$02_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$02 text$04 if$02 repeat$13 variant$12 _", function tag$02_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\">${item}</template><template else repeat.for=\"item of ['a', 'b', 'c']\">${item}</template></template>";
        const name = "app";
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$01 _", function tag$03_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$02 _", function tag$03_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$03 _", function tag$03_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$04 _", function tag$03_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$05 _", function tag$03_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$06 _", function tag$03_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$07 _", function tag$03_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$08 _", function tag$03_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$08$empty _", function tag$03_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$09 _", function tag$03_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$11 variant$09$empty _", function tag$03_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$01 _", function tag$03_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$02 _", function tag$03_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$03 _", function tag$03_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$04 _", function tag$03_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$05 _", function tag$03_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$06 _", function tag$03_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$07 _", function tag$03_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$08 _", function tag$03_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$08$empty _", function tag$03_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$09 _", function tag$03_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$12 variant$09$empty _", function tag$03_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$01 _", function tag$03_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$02 _", function tag$03_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$03 _", function tag$03_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$04 _", function tag$03_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$05 _", function tag$03_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$06 _", function tag$03_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$07 _", function tag$03_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$08 _", function tag$03_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$08$empty _", function tag$03_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$09 _", function tag$03_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$01 repeat$13 variant$09$empty _", function tag$03_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$01 _", function tag$03_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$02 _", function tag$03_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$03 _", function tag$03_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$10 _", function tag$03_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$10$empty _", function tag$03_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$11 _", function tag$03_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$11$empty _", function tag$03_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$11 variant$12 _", function tag$03_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$01 _", function tag$03_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$02 _", function tag$03_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$03 _", function tag$03_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$10 _", function tag$03_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$10$empty _", function tag$03_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$11 _", function tag$03_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$11$empty _", function tag$03_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$12 variant$12 _", function tag$03_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$01 _", function tag$03_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$02 _", function tag$03_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$03 _", function tag$03_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$10 _", function tag$03_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$10$empty _", function tag$03_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$11 _", function tag$03_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$11$empty _", function tag$03_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$03 if$02 repeat$13 variant$12 _", function tag$03_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$01 _", function tag$03_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$02 _", function tag$03_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$03 _", function tag$03_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$04 _", function tag$03_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$05 _", function tag$03_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$06 _", function tag$03_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$07 _", function tag$03_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$08 _", function tag$03_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$08$empty _", function tag$03_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$09 _", function tag$03_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$11 variant$09$empty _", function tag$03_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$01 _", function tag$03_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$02 _", function tag$03_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$03 _", function tag$03_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$04 _", function tag$03_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$05 _", function tag$03_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$06 _", function tag$03_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$07 _", function tag$03_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$08 _", function tag$03_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$08$empty _", function tag$03_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$09 _", function tag$03_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$12 variant$09$empty _", function tag$03_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$01 _", function tag$03_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$02 _", function tag$03_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$03 _", function tag$03_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$04 _", function tag$03_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$05 _", function tag$03_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$06 _", function tag$03_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$07 _", function tag$03_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$08 _", function tag$03_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$08$empty _", function tag$03_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$09 _", function tag$03_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$01 repeat$13 variant$09$empty _", function tag$03_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$01 _", function tag$03_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$02 _", function tag$03_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$03 _", function tag$03_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$10 _", function tag$03_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$10$empty _", function tag$03_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$11 _", function tag$03_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$11$empty _", function tag$03_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$11 variant$12 _", function tag$03_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$01 _", function tag$03_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$02 _", function tag$03_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$03 _", function tag$03_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$10 _", function tag$03_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$10$empty _", function tag$03_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$11 _", function tag$03_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$11$empty _", function tag$03_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$12 variant$12 _", function tag$03_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$01 _", function tag$03_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$02 _", function tag$03_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$03 _", function tag$03_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$10 _", function tag$03_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$10$empty _", function tag$03_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$11 _", function tag$03_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$11$empty _", function tag$03_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$03 text$04 if$02 repeat$13 variant$12 _", function tag$03_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$01 _", function tag$04_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$02 _", function tag$04_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$03 _", function tag$04_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$04 _", function tag$04_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$05 _", function tag$04_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$06 _", function tag$04_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$07 _", function tag$04_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$08 _", function tag$04_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$08$empty _", function tag$04_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$09 _", function tag$04_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$11 variant$09$empty _", function tag$04_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$01 _", function tag$04_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$02 _", function tag$04_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$03 _", function tag$04_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$04 _", function tag$04_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$05 _", function tag$04_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$06 _", function tag$04_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$07 _", function tag$04_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$08 _", function tag$04_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$08$empty _", function tag$04_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$09 _", function tag$04_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$12 variant$09$empty _", function tag$04_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$01 _", function tag$04_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$02 _", function tag$04_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$03 _", function tag$04_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$04 _", function tag$04_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$05 _", function tag$04_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$06 _", function tag$04_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$07 _", function tag$04_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$08 _", function tag$04_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$08$empty _", function tag$04_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$09 _", function tag$04_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$01 repeat$13 variant$09$empty _", function tag$04_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$01 _", function tag$04_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$02 _", function tag$04_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$03 _", function tag$04_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$10 _", function tag$04_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$10$empty _", function tag$04_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$11 _", function tag$04_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$11$empty _", function tag$04_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$11 variant$12 _", function tag$04_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$01 _", function tag$04_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$02 _", function tag$04_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$03 _", function tag$04_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$10 _", function tag$04_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$10$empty _", function tag$04_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$11 _", function tag$04_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$11$empty _", function tag$04_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$12 variant$12 _", function tag$04_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$01 _", function tag$04_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$02 _", function tag$04_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$03 _", function tag$04_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$10 _", function tag$04_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$10$empty _", function tag$04_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$11 _", function tag$04_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$11$empty _", function tag$04_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$03 if$02 repeat$13 variant$12 _", function tag$04_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$01 _", function tag$04_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$02 _", function tag$04_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$03 _", function tag$04_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$04 _", function tag$04_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$05 _", function tag$04_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$06 _", function tag$04_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$07 _", function tag$04_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$08 _", function tag$04_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$08$empty _", function tag$04_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$09 _", function tag$04_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$11 variant$09$empty _", function tag$04_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$01 _", function tag$04_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$02 _", function tag$04_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$03 _", function tag$04_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$04 _", function tag$04_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$05 _", function tag$04_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$06 _", function tag$04_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$07 _", function tag$04_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$08 _", function tag$04_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$08$empty _", function tag$04_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$09 _", function tag$04_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$12 variant$09$empty _", function tag$04_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$01 _", function tag$04_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$02 _", function tag$04_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$03 _", function tag$04_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$04 _", function tag$04_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$05 _", function tag$04_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$06 _", function tag$04_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$07 _", function tag$04_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$08 _", function tag$04_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$08$empty _", function tag$04_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$09 _", function tag$04_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$01 repeat$13 variant$09$empty _", function tag$04_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$01 _", function tag$04_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$02 _", function tag$04_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$03 _", function tag$04_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$10 _", function tag$04_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$10$empty _", function tag$04_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$11 _", function tag$04_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$11$empty _", function tag$04_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$11 variant$12 _", function tag$04_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$01 _", function tag$04_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$02 _", function tag$04_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$03 _", function tag$04_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$10 _", function tag$04_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$10$empty _", function tag$04_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$11 _", function tag$04_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$11$empty _", function tag$04_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$12 variant$12 _", function tag$04_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$01 _", function tag$04_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$02 _", function tag$04_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$03 _", function tag$04_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$10 _", function tag$04_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$10$empty _", function tag$04_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$11 _", function tag$04_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$11$empty _", function tag$04_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$04 text$04 if$02 repeat$13 variant$12 _", function tag$04_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static containerless = true;
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$01 _", function tag$05_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$02 _", function tag$05_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$03 _", function tag$05_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$04 _", function tag$05_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$05 _", function tag$05_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$06 _", function tag$05_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$07 _", function tag$05_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$08 _", function tag$05_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$08$empty _", function tag$05_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$09 _", function tag$05_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$11 variant$09$empty _", function tag$05_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$01 _", function tag$05_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$02 _", function tag$05_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$03 _", function tag$05_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$04 _", function tag$05_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$05 _", function tag$05_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$06 _", function tag$05_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$07 _", function tag$05_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$08 _", function tag$05_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$08$empty _", function tag$05_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$09 _", function tag$05_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$12 variant$09$empty _", function tag$05_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$01 _", function tag$05_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$02 _", function tag$05_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$03 _", function tag$05_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$04 _", function tag$05_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$05 _", function tag$05_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$06 _", function tag$05_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$07 _", function tag$05_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$08 _", function tag$05_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$08$empty _", function tag$05_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$09 _", function tag$05_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$01 repeat$13 variant$09$empty _", function tag$05_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$01 _", function tag$05_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$02 _", function tag$05_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$03 _", function tag$05_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$10 _", function tag$05_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$10$empty _", function tag$05_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$11 _", function tag$05_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$11$empty _", function tag$05_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$11 variant$12 _", function tag$05_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$01 _", function tag$05_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$02 _", function tag$05_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$03 _", function tag$05_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$10 _", function tag$05_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$10$empty _", function tag$05_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$11 _", function tag$05_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$11$empty _", function tag$05_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$12 variant$12 _", function tag$05_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$01 _", function tag$05_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$02 _", function tag$05_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$03 _", function tag$05_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$10 _", function tag$05_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$10$empty _", function tag$05_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$11 _", function tag$05_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$11$empty _", function tag$05_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$03 if$02 repeat$13 variant$12 _", function tag$05_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$01 _", function tag$05_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$02 _", function tag$05_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$03 _", function tag$05_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$04 _", function tag$05_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$05 _", function tag$05_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$06 _", function tag$05_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$07 _", function tag$05_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$08 _", function tag$05_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$08$empty _", function tag$05_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$09 _", function tag$05_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$11 variant$09$empty _", function tag$05_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$01 _", function tag$05_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$02 _", function tag$05_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$03 _", function tag$05_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$04 _", function tag$05_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$05 _", function tag$05_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$06 _", function tag$05_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$07 _", function tag$05_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$08 _", function tag$05_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$08$empty _", function tag$05_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$09 _", function tag$05_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$12 variant$09$empty _", function tag$05_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$01 _", function tag$05_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$02 _", function tag$05_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$03 _", function tag$05_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$04 _", function tag$05_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$05 _", function tag$05_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$06 _", function tag$05_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$07 _", function tag$05_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$08 _", function tag$05_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$08$empty _", function tag$05_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$09 _", function tag$05_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$01 repeat$13 variant$09$empty _", function tag$05_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$01 _", function tag$05_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$02 _", function tag$05_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$03 _", function tag$05_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$10 _", function tag$05_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$10$empty _", function tag$05_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$11 _", function tag$05_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$11$empty _", function tag$05_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$11 variant$12 _", function tag$05_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$01 _", function tag$05_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$02 _", function tag$05_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$03 _", function tag$05_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$10 _", function tag$05_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$10$empty _", function tag$05_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$11 _", function tag$05_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$11$empty _", function tag$05_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$12 variant$12 _", function tag$05_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$01 _", function tag$05_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$02 _", function tag$05_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$03 _", function tag$05_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$10 _", function tag$05_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$10$empty _", function tag$05_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$11 _", function tag$05_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$11$empty _", function tag$05_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$05 text$04 if$02 repeat$13 variant$12 _", function tag$05_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "open" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$01 _", function tag$06_text$03_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$02 _", function tag$06_text$03_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$03 _", function tag$06_text$03_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$04 _", function tag$06_text$03_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$05 _", function tag$06_text$03_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$06 _", function tag$06_text$03_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$07 _", function tag$06_text$03_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$08 _", function tag$06_text$03_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$08$empty _", function tag$06_text$03_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$09 _", function tag$06_text$03_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$11 variant$09$empty _", function tag$06_text$03_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$01 _", function tag$06_text$03_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$02 _", function tag$06_text$03_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$03 _", function tag$06_text$03_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$04 _", function tag$06_text$03_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$05 _", function tag$06_text$03_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$06 _", function tag$06_text$03_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$07 _", function tag$06_text$03_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$08 _", function tag$06_text$03_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$08$empty _", function tag$06_text$03_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$09 _", function tag$06_text$03_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$12 variant$09$empty _", function tag$06_text$03_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$01 _", function tag$06_text$03_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$02 _", function tag$06_text$03_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$03 _", function tag$06_text$03_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$04 _", function tag$06_text$03_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$05 _", function tag$06_text$03_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$06 _", function tag$06_text$03_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$07 _", function tag$06_text$03_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$08 _", function tag$06_text$03_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$08$empty _", function tag$06_text$03_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$09 _", function tag$06_text$03_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$01 repeat$13 variant$09$empty _", function tag$06_text$03_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$01 _", function tag$06_text$03_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$02 _", function tag$06_text$03_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$03 _", function tag$06_text$03_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$10 _", function tag$06_text$03_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$10$empty _", function tag$06_text$03_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$11 _", function tag$06_text$03_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$11$empty _", function tag$06_text$03_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$11 variant$12 _", function tag$06_text$03_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$01 _", function tag$06_text$03_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$02 _", function tag$06_text$03_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$03 _", function tag$06_text$03_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$10 _", function tag$06_text$03_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$10$empty _", function tag$06_text$03_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$11 _", function tag$06_text$03_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$11$empty _", function tag$06_text$03_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$12 variant$12 _", function tag$06_text$03_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$01 _", function tag$06_text$03_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$02 _", function tag$06_text$03_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$03 _", function tag$06_text$03_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$10 _", function tag$06_text$03_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$10$empty _", function tag$06_text$03_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$11 _", function tag$06_text$03_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$11$empty _", function tag$06_text$03_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$03 if$02 repeat$13 variant$12 _", function tag$06_text$03_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$01 _", function tag$06_text$04_if$01_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$02 _", function tag$06_text$04_if$01_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$03 _", function tag$06_text$04_if$01_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$04 _", function tag$06_text$04_if$01_repeat$11_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$05 _", function tag$06_text$04_if$01_repeat$11_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$06 _", function tag$06_text$04_if$01_repeat$11_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$07 _", function tag$06_text$04_if$01_repeat$11_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$08 _", function tag$06_text$04_if$01_repeat$11_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$08$empty _", function tag$06_text$04_if$01_repeat$11_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$09 _", function tag$06_text$04_if$01_repeat$11_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$11 variant$09$empty _", function tag$06_text$04_if$01_repeat$11_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("a", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$01 _", function tag$06_text$04_if$01_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$02 _", function tag$06_text$04_if$01_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$03 _", function tag$06_text$04_if$01_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$04 _", function tag$06_text$04_if$01_repeat$12_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$05 _", function tag$06_text$04_if$01_repeat$12_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\" if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$06 _", function tag$06_text$04_if$01_repeat$12_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$07 _", function tag$06_text$04_if$01_repeat$12_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$08 _", function tag$06_text$04_if$01_repeat$12_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$08$empty _", function tag$06_text$04_if$01_repeat$12_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"true\"><foo msg.bind=\"msg\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$09 _", function tag$06_text$04_if$01_repeat$12_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$12 variant$09$empty _", function tag$06_text$04_if$01_repeat$12_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("aaa", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$01 _", function tag$06_text$04_if$01_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$02 _", function tag$06_text$04_if$01_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$03 _", function tag$06_text$04_if$01_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$04 _", function tag$06_text$04_if$01_repeat$13_variant$04__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$05 _", function tag$06_text$04_if$01_repeat$13_variant$05__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\" if.bind=\"true\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$06 _", function tag$06_text$04_if$01_repeat$13_variant$06__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$07 _", function tag$06_text$04_if$01_repeat$13_variant$07__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$08 _", function tag$06_text$04_if$01_repeat$13_variant$08__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$08$empty _", function tag$06_text$04_if$01_repeat$13_variant$08$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"true\"><foo item.bind=\"item\"></foo></template><template else></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$09 _", function tag$06_text$04_if$01_repeat$13_variant$09__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$01 repeat$13 variant$09$empty _", function tag$06_text$04_if$01_repeat$13_variant$09$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"true\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$01 _", function tag$06_text$04_if$02_repeat$11_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$02 _", function tag$06_text$04_if$02_repeat$11_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$03 _", function tag$06_text$04_if$02_repeat$11_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 1\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$10 _", function tag$06_text$04_if$02_repeat$11_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$10$empty _", function tag$06_text$04_if$02_repeat$11_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 1\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$11 _", function tag$06_text$04_if$02_repeat$11_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$11$empty _", function tag$06_text$04_if$02_repeat$11_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$11 variant$12 _", function tag$06_text$04_if$02_repeat$11_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 1\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("b", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$01 _", function tag$06_text$04_if$02_repeat$12_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$02 _", function tag$06_text$04_if$02_repeat$12_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$03 _", function tag$06_text$04_if$02_repeat$12_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"i of 3\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$10 _", function tag$06_text$04_if$02_repeat$12_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$10$empty _", function tag$06_text$04_if$02_repeat$12_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"i of 3\"><template if.bind=\"false\"></template><template else><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$11 _", function tag$06_text$04_if$02_repeat$12_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$11$empty _", function tag$06_text$04_if$02_repeat$12_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$12 variant$12 _", function tag$06_text$04_if$02_repeat$12_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo msg.bind=\"msg\"></foo></template><template else repeat.for=\"i of 3\"><foo not.bind=\"not\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("bbb", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$01 _", function tag$06_text$04_if$02_repeat$13_variant$01__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$02 _", function tag$06_text$04_if$02_repeat$13_variant$02__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$03 _", function tag$06_text$04_if$02_repeat$13_variant$03__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\" repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$10 _", function tag$06_text$04_if$02_repeat$13_variant$10__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$10$empty _", function tag$06_text$04_if$02_repeat$13_variant$10$empty__() {
        const { au, host } = setup();
        const template = "<template><template repeat.for=\"item of ['a', 'b', 'c']\"><template if.bind=\"false\"></template><template else><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$11 _", function tag$06_text$04_if$02_repeat$13_variant$11__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$11$empty _", function tag$06_text$04_if$02_repeat$13_variant$11$empty__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"></template><template else><template repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
    it("tag$06 text$04 if$02 repeat$13 variant$12 _", function tag$06_text$04_if$02_repeat$13_variant$12__() {
        const { au, host } = setup();
        const template = "<template><template if.bind=\"false\"><foo item.bind=\"item\"></foo></template><template else repeat.for=\"item of ['a', 'b', 'c']\"><foo item.bind=\"item\"></foo></template></template>";
        const name = "app";
        const Foo = CustomElementResource.define({ name: "foo", template: "<template>${msg}${not}${item}</template>" }, class {
            static bindables = { msg: { attribute: "msg", property: "msg" }, not: { attribute: "not", property: "not" }, item: { attribute: "item", property: "item" } };
            static shadowOptions = { mode: "closed" };
            msg = "";
            not = "";
            item = "";
        });
        au.register(Foo);
        const App = CustomElementResource.define({ name, template }, class {
            msg = "a";
            not = "b";
        });
        const component = new App();
        au.app({ host, component });
        au.start();
        const outerHtmlAfterStart1 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #1");
        au.stop();
        const outerHtmlAfterStop1 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #1");
        au.start();
        const outerHtmlAfterStart2 = host.outerHTML;
        expect(host.textContent).to.equal("abc", "after start #2");
        au.stop();
        const outerHtmlAfterStop2 = host.outerHTML;
        expect(host.textContent).to.equal("", "after stop #2");
        expect(outerHtmlAfterStart1).to.equal(outerHtmlAfterStart2, "outerHTML after start #1 / #2");
        expect(outerHtmlAfterStop1).to.equal(outerHtmlAfterStop2, "outerHTML after stop #1 / #2");
    });
});