/**
 *
 * NOIEndpointE: This file is still WIP and will go through at least one more iteration of refactoring, commenting and clean up!
 * In its current state, it is NOT a good source for learning about the inner workings and design of the router.
 *
 */
/* eslint-disable @typescript-eslint/no-empty-function */
import { Navigation } from './navigation.js';
import { OpenPromise } from './utilities/open-promise.js';

export class StateCoordinator<IEndpoint, NavigationState> {
  // public constructor(@ILogger private readonly logger: ILogger) {
  //   this.logger = logger.root.scopeTo('StateCoordinator');
  //   this.logger.trace('constructor()');
  // }


}
