import { expect } from 'chai';
import { Viewport } from '../../src/index';

describe('Viewport', () => {
  it('can be created', () => {
    const sut = new Viewport(null, null, null, null, null);
  });
});
