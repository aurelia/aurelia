# Sierpinski Triangle

## Getting started

To install and run the app (in watch mode):

```bash
npm install
npm build -- -w
# 2nd
npm run serve
```

## build production

```bash
npm run build -- --mode production
```
